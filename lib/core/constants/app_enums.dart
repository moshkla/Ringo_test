enum LoadingState { initial, loading, success, error }

enum TransAnimType { scale, rotate, size, fade }

enum CardCompany { amex, visa, master, mada }

enum OperationType { order, subscription, edit }

enum DialogType {warning, question, error,success}
